interface:
  display_name: Implement with Graph
  short_description: Execute through concise, verified task graphs
  default_prompt: Use $implement-with-graph to execute this change through a concise
    terminal-style graph with code, regression, and required visual-runtime proof.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
